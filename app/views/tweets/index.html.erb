<h1>TWEETS</h1>

<table>
  <tr>
    <th>tweet</th>
  <tr/>

  <% @tweets.each do |tweet| %>
    <tr>
      <td><%= tweet.content %></td>
      <td><%= link_to '編集', edit_tweet_path(tweet.id) %>
    </tr>
  <% end %>
</table>

<%= link_to "new", new_tweet_path %>
